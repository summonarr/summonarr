// Empty stand-in for the bundler-provided "server-only" / "client-only" marker
// packages, mapped in by tests/_loader.mjs. The markers only matter to the
// bundler at build time; in these Node tests they do nothing.
export {};
