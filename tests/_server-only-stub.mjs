// Empty stand-in for the bundler-provided "server-only" marker package, mapped
// in by tests/_loader.mjs. Unit tests run in Node — the server side — so the
// marker's build-time guarantee is vacuously satisfied.
export {};
