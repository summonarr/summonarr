// Status types shared by the settings form files (extracted from settings-ui.tsx).
export type SaveStatus = "idle" | "saving" | "ok" | "error";
export type LoadStatus = "idle" | "loading" | "loaded" | "error";
