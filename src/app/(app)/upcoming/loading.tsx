// Skeleton for the Upcoming grid while TMDB + cache lookups run.
import { PosterGridSkeleton } from "@/components/loading/poster-grid-skeleton";

export default function Loading() {
  return <PosterGridSkeleton title="Upcoming" subtitle="Movies and shows arriving soon" />;
}
